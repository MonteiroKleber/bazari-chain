[package]
name = "pallet-bazari-attestation"
version = "0.1.0"
authors = ["Bazari Team"]
edition = "2021"
license = "MIT-0"
publish = false
repository = "https://github.com/bazari-labs/bazari-chain"

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
codec = { workspace = true }
scale-info = { workspace = true }
serde = { version = "1.0", default-features = false, features = ["derive", "alloc"] }

# Substrate FRAME
frame-support = { workspace = true }
frame-system = { workspace = true }
sp-core = { workspace = true }
sp-io = { workspace = true }
sp-runtime = { workspace = true }

# Bazari pallets
pallet-bazari-commerce = { path = "../bazari-commerce", default-features = false }

[dev-dependencies]
sp-core = { workspace = true, features = ["std"] }
sp-io = { workspace = true, features = ["std"] }
pallet-balances = { workspace = true, features = ["std"] }
pallet-uniques = { workspace = true, features = ["std"] }
pallet-stores = { path = "../stores", default-features = false, features = ["std"] }

[features]
default = ["std"]
std = [
	"codec/std",
	"scale-info/std",
	"serde/std",
	"frame-support/std",
	"frame-system/std",
	"sp-core/std",
	"sp-io/std",
	"sp-runtime/std",
	"pallet-bazari-commerce/std",
]
try-runtime = [
	"frame-support/try-runtime",
	"frame-system/try-runtime",
	"pallet-bazari-commerce/try-runtime",
]
runtime-benchmarks = [
	"frame-support/runtime-benchmarks",
	"frame-system/runtime-benchmarks",
	"pallet-bazari-commerce/runtime-benchmarks",
]
