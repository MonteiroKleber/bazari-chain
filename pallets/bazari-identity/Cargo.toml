[package]
name = "pallet-bazari-identity"
version = "0.1.0"
description = "Soulbound NFT profile pallet with reputation and badges for Bazari"
authors = ["Bazari Team"]
edition = "2021"
license = "MIT-0"
publish = false

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
codec = { package = "parity-scale-codec", version = "3.7.4", default-features = false, features = ["derive"] }
scale-info = { version = "2.11.6", default-features = false, features = ["derive"] }
frame-support = { version = "40.1.0", default-features = false }
frame-system = { version = "40.1.0", default-features = false }
sp-runtime = { version = "41.1.0", default-features = false }

[features]
default = ["std"]
std = [
    "codec/std",
    "scale-info/std",
    "frame-support/std",
    "frame-system/std",
    "sp-runtime/std",
]

[dev-dependencies]
sp-core = { version = "36.1.0", default-features = false, features = ["std"] }
sp-io = { version = "40.0.1", default-features = false, features = ["std"] }
sp-runtime = { version = "41.1.0", default-features = false, features = ["std"] }
pallet-balances = { version = "41.1.0", default-features = false, features = ["std"] }
