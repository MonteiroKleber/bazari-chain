[package]
name = "pallet-bazari-dispute"
version = "0.1.0"
edition = "2021"
license = "MIT-0"
publish = false

[dependencies]
codec = { workspace = true }
scale-info = { workspace = true }
serde = { version = "1.0", default-features = false, features = ["derive", "alloc"] }
frame-support = { workspace = true }
frame-system = { workspace = true }
sp-runtime = { workspace = true }
sp-core = { workspace = true }
sp-io = { workspace = true }
pallet-bazari-identity = { path = "../bazari-identity", default-features = false }
pallet-bazari-commerce = { path = "../bazari-commerce", default-features = false }
pallet-bazari-escrow = { path = "../bazari-escrow", default-features = false }
pallet-bazari-fulfillment = { path = "../bazari-fulfillment", default-features = false }
pallet-insecure-randomness-collective-flip = { version = "28.0.0", default-features = false }

[dev-dependencies]
sp-core = { workspace = true, features = ["std"] }
sp-io = { workspace = true, features = ["std"] }
pallet-balances = { workspace = true, features = ["std"] }
pallet-uniques = { workspace = true, features = ["std"] }
pallet-stores = { workspace = true, features = ["std"] }
pallet-bazari-identity = { path = "../bazari-identity", default-features = false, features = ["std"] }
pallet-bazari-affiliate = { path = "../bazari-affiliate", default-features = false, features = ["std"] }

[features]
default = ["std"]
std = [
	"codec/std",
	"scale-info/std",
	"serde/std",
	"frame-support/std",
	"frame-system/std",
	"sp-runtime/std",
	"sp-core/std",
	"sp-io/std",
	"pallet-bazari-identity/std",
	"pallet-bazari-commerce/std",
	"pallet-bazari-escrow/std",
	"pallet-bazari-fulfillment/std",
	"pallet-insecure-randomness-collective-flip/std",
]
runtime-benchmarks = [
	"frame-support/runtime-benchmarks",
	"frame-system/runtime-benchmarks",
]
try-runtime = [
	"frame-support/try-runtime",
	"frame-system/try-runtime",
]
